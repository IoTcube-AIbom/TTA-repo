# Configuration

## Config files

- Default: `configs/default.yaml`
- Development: `configs/dev.yaml`

## Environment variables

- `TOOL_LOG_LEVEL` (default: `info`)
- `TOOL_CONFIG_PATH` (optional path to config file)

## Precedence

1. CLI flags
2. Environment variables
3. Config file
4. Built-in defaults
